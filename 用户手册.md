# Slides Timer 用户手册

## 1. 项目简介

Slides Timer 是一款专为演示文稿设计的计时工具，支持 Microsoft PowerPoint、WPS 和 LibreOffice Impress。它能够自动检测幻灯片放映状态，根据不同的 PPT 文件自动加载预设的演示时间，帮助控制会议时长。

### 主要功能

- **PPT 文件预设时间**：为不同的 PPT 文件设置不同的演示时间
- **自动加载时间**：开始放映时自动检测当前 PPT 文件并加载预设时间
- **开始计时提示**：显示"开始计时：X分钟"的横幅提示
- **时间警告**：当时间剩余1/3时显示警告提示
- **时间到提示**：当时间耗尽时显示时间到的提示
- **跨平台支持**：支持 Windows 和 Linux（麒麟 v10）

## 2. 安装方法

### Windows

#### 方法一：使用已打包的可执行文件
1. 打开 `dist/SlidesTimer` 目录
2. 双击 `SlidesTimer.exe` 文件即可运行

#### 方法二：从源码运行
1. 确保系统已安装 Python 3.10 或更高版本
2. 安装依赖：
   ```
   pip install -r requirements.txt
   ```
3. 运行：
   ```
   python main.py
   ```

### Linux（麒麟 v10）

#### 方法一：安装 deb 包
1. 从 GitHub Releases 下载对应架构的 deb 包：
   - `slides-timer_1.0.0_amd64.deb`（x86_64 架构）
   - `slides-timer_1.0.0_arm64.deb`（ARM 架构）
2. 安装：
   ```bash
   sudo dpkg -i slides-timer_1.0.0_amd64.deb
   sudo apt-get install -f  # 修复依赖
   ```
3. 安装后在应用菜单中找到"演讲计时助手"即可启动

#### 方法二：从源码运行
1. 安装系统依赖：
   ```bash
   sudo apt install python3-pip xdotool
   ```
2. 安装 Python 依赖：
   ```bash
   pip3 install -r requirements.txt
   ```
3. 运行：
   ```bash
   python3 main.py
   ```

## 3. 使用方法

### 3.1 预设 PPT 文件时间

1. **打开设置窗口**：在系统托盘找到 Slides Timer 图标，右键点击并选择"设置"
2. **进入 PPT 文件管理**：在设置窗口中点击"PPT文件管理"选项卡
3. **添加 PPT 文件**：
   - 点击"浏览"按钮选择 PPT 文件
   - 设置演示时间（分钟）
   - 点击"添加"按钮
4. **管理 PPT 文件**：
   - **修改时间**：点击对应 PPT 文件右侧的"修改"按钮
   - **删除文件**：点击对应 PPT 文件右侧的"删除"按钮
   - **一键清空**：点击右上角的"一键清空"按钮

### 3.2 开始演示

1. **打开 PPT 文件**：在演示软件中打开需要演示的 PPT 文件
2. **开始放映**：点击幻灯片放映按钮开始演示
3. **自动计时**：系统会自动检测当前 PPT 文件并加载预设时间
4. **查看剩余时间**：系统会在时间剩余1/3时显示警告提示，时间耗尽时显示时间到的提示

### 3.3 手动设置时间

如果 PPT 文件没有预设时间，系统会弹出时间设置窗口：

1. **设置时间**：使用 +/- 按钮或直接输入演示时间（分钟）
2. **快速选择**：点击5、10、15、20分钟的快速选择按钮
3. **开始计时**：点击"开始计时"按钮开始计时

## 4. 界面说明

### 4.1 系统托盘图标

- **左键点击**：打开设置窗口
- **右键点击**：显示菜单，包含"设置"和"退出"选项

### 4.2 设置窗口

- **提醒设置**：设置警告提示和时间到提示的内容、持续时间、触发条件和声音
- **外观与位置**：设置提示横幅的位置、偏移、字体、颜色等
- **PPT文件管理**：管理 PPT 文件的预设时间

### 4.3 PPT 文件管理窗口

- **添加 PPT 文件**：选择 PPT 文件并设置时间
- **PPT 文件列表**：显示所有预设时间的 PPT 文件
- **一键清空**：清空所有 PPT 文件的预设时间

## 5. 注意事项

1. **PPT 文件路径**：系统会根据 PPT 文件的完整路径来识别文件，移动文件后需要重新设置时间
2. **声音文件**：支持 WAV 和 MP3 格式
3. **Linux 检测**：在 Linux 上通过窗口标题检测放映状态，需要安装 `xdotool`
4. **跨平台字体**：不同系统上可用字体不同，建议使用系统默认字体

## 6. 故障排除

### 6.1 Linux 上无法检测 PPT 放映

- 确保已安装 `xdotool`：`sudo apt install xdotool`
- 检查演示软件窗口标题是否包含"放映"或"Slideshow"关键词
- 如果自动检测不工作，可以使用手动模式设置时间

### 6.2 预设时间不生效

- 检查 PPT 文件路径是否与预设时的路径一致
- 检查 PPT 文件是否已添加到管理列表中

### 6.3 声音不播放

- **Linux**：确保已安装 `aplay` 或 `paplay`
- 检查声音文件路径是否正确
- 检查系统音量是否开启

## 7. 常见问题

**Q: 可以为不同的 PPT 文件设置不同的时间吗？**
A: 是的，您可以在 PPT 文件管理中为每个文件设置不同时间。

**Q: 支持哪些演示软件？**
A: Windows 上支持 Microsoft PowerPoint 和 WPS；Linux 上支持 WPS、LibreOffice Impress 等，通过窗口标题检测放映状态。

**Q: 如何在麒麟 v10 上安装？**
A: 下载对应架构的 deb 包，使用 `sudo dpkg -i` 安装即可。

**Q: 如何卸载？**
A: Linux 上使用 `sudo apt remove slides-timer`；Windows 上删除文件夹即可。

## 8. 构建说明

### 构建 deb 包

1. Fork 本仓库
2. 推送一个 tag（如 `v1.0.0`）触发 GitHub Action 自动构建
3. 在 Releases 页面下载 deb 包

### 本地构建

```bash
# 安装 PyInstaller
pip install pyinstaller

# 构建可执行文件
pyinstaller --onefile --name slides-timer main.py

# 构建 deb 包
bash scripts/build-deb.sh amd64
```

---

本用户手册涵盖了 Slides Timer 的所有主要功能和使用方法。如有任何建议或反馈，欢迎随时联系开发者。
